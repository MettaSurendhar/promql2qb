This folder will hold PromQL -> SigNoz Query Builder JSON example pairs,
used both as documentation and as fixtures for table-driven tests.

Planned format, one pair per PromQL feature:

  examples/01-simple-filter.promql
  examples/01-simple-filter.json
  examples/02-aggregation.promql
  examples/02-aggregation.json
  ...

Not yet populated — added as each conversion feature is implemented.
